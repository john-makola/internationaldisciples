// src/types.ts

// Event interface used for listing or displaying conference events
export interface ConferenceEvent {
  id: number;
  title: string;
  description: string;
  date: string;
  image: string;
  category: string;
}

